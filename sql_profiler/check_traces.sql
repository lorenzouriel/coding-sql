-- ========================================================
-- Check if any trace is currently running
-- ========================================================
-- sys.traces lists all active traces on the server, including default traces.
SELECT * 
FROM sys.traces;